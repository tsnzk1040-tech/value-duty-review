# Gemini（キーはリポ外。値はローカルの .env.local に置く）
GEMINI_API_KEY=
# 任意。未設定時は gemini-3.8-flash
GEMINI_MODEL=gemini-3.8-flash
# auto（既定）| gemini | stub — auto はキーがあれば Gemini、無ければ stub
REVIEW_GENERATE_PROVIDER=auto
